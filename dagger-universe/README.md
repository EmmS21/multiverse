# Dagger universe

A map of Dagger drawn from GitHub issues. Same sky engine as LiveKit. Separate production site.

- [Helper prepare](https://emms21.github.io/dagger-universe/) (#13931) — they set GOPROXY or git on the laptop; load module still fails in the helper container.
- [Simulation entry](https://emms21.github.io/dagger-universe/dagger/) — same gate via `/dagger/`.

Galaxies: engine → module → call → run → cache → secrets → uncharted.
